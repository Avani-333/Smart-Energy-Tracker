[build]
builder = "NIXPACKS"

[deploy]
startCommand = "streamlit run src/app.py --server.address 0.0.0.0 --server.port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
